import * as Feature from "./index";

describe("feature", () => {
  describe("sum", () => {
    it("should ", () => {
      const result = Feature.sum(10, 20);

      expect(result).toBe(30);
    });
  });
});
